[package]
name = "sdk_fpvm"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^2.1.0", default-features = false, features = ["std", "heap-embedded-alloc", "unstable", "getrandom"] }
solana-sdk = { path = "../../../" }